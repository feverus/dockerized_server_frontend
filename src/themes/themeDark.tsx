import { createTheme } from '@mui/material/styles'

export const themeDark = createTheme({
    palette: {
        background: {
            default: '#222222',
        },
        text: {
            primary: '#ffffff',
        },
    },
})
