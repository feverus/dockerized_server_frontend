import { createTheme } from '@mui/material/styles'

export const themeLight = createTheme({
    palette: {
        background: {
            default: '#f3f6f9',
        },
        text: {
            primary: '#000000',
        },
    },
});
